# Serialización de valores primitivos

Convertir un valor simple (tipo primitivo) a su representación en string, para poder almacenarlo o transmitirlo como texto.

Ejemplos:
- Un `int` a `string` (`42` → `"42"`)
- Un `Date` a `string` (fecha → `"2026-08-31T00:00:00Z"`, formato ISO)
