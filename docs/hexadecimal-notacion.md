# Notación hexadecimal

Representación compacta de binario, muy usada para direcciones de memoria, colores y bytes.

**Ejemplos:**
1. C#: `Convert.ToString(42, 16)` → `"2a"`.
2. JavaScript: `(42).toString(16)` → `"2a"`.
3. Comando: `xxd archivo.bin` — ver el contenido de un archivo en hexadecimal.
