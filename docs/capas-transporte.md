# Capa de transporte

Entrega de datos entre procesos, con o sin garantía de orden y confiabilidad (TCP/UDP).

**Ejemplos:**
1. Java: `new Socket(host, port)` — conexión TCP.
2. C#: `new UdpClient()` — conexión UDP.
3. Comando `netstat -an` — ver las conexiones activas por puerto.
