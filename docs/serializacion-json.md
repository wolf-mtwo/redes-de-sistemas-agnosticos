# JSON

Formato de texto basado en pares clave-valor, el más usado hoy para APIs web.

**Ejemplos:**
1. JavaScript: `JSON.stringify({nombre: "Ana"})`.
2. C#: `System.Text.Json.JsonSerializer.Serialize(objeto)`.
3. Comando: `curl https://api.example.com | jq .` — ver y formatear una respuesta JSON.
